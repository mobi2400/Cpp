#include <iostream>
using namespace std;
int main()
{

    int v, u float distance, a;
    cout << "enter the value of V U A";
    cin >> v >> u >> a;
    distance = (v * v - u * u) / (2 * a);
    cout << "the distance is" << distance;
    return 0;
}
